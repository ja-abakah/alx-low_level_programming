#include "main.h"
/**
 * add - Entry point of the program
 *
 * Description: This program adds two numbers.
 * @X: this stores the first numner
 * @Y: stores the second number
 * Return: 0 (Success)
 */
int add(int X, int Y)
{
int Z;

Z = X + Y;

return (Z);
}
