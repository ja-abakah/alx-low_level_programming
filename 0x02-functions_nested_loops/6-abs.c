#include "main.h"
/**
 * _abs - Entry point of the program
 *
 * Description: Prints the absolute number.
 *@Absolute: the variable to check
 *
 * Return: Absolute (Success)
 */
int _abs(int Absolute)
{
if (Absolute >= 0)
{
return (Absolute);
}

return (-Absolute);
}
